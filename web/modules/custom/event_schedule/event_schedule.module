<?php

/**
 * @file
 * Contains event_schedule.module.
 */

/**
 * Implements hook_theme.
 */
function event_schedule_theme($existing, $type, $theme, $path) {
  return [
    'schedule' => [
      'variables' => [
        'title' => NULL,
      ],
      'template' => 'schedule',
    ],
  ];
}
